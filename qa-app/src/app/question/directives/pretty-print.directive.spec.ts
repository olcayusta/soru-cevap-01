import { PrettyPrintDirective } from './pretty-print.directive';

describe('PrettyPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new PrettyPrintDirective();
    expect(directive).toBeTruthy();
  });
});
